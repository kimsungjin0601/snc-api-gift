<!-- <?xml version=“1.0” encoding=“UTF-8” ?> -->
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.snc.gift.mapper.TenantMapper">
    <select id="getTenantByDefault" resultType="TenantVo">
        select t1.tenant_no, tenant_code, tenant_name, timezone, locale, currency_code, is_deleted, created_at, created_by, updated_at, updated_by
        from tenant t1
        where t1.is_deleted = false
        limit 1
    </select>
</mapper>